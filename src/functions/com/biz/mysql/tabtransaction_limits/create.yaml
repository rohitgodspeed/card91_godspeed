summary: Create tabTransaction_Limits
tasks:
  - id: mysql_tabtransaction_limits_create
    fn: com.gs.datastore
    args:
      datasource: mysql
      data:
        data: <% inputs.body %>
      config:
        method: tabTransaction_Limits.create
