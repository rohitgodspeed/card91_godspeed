summary: Fetch many tabKYC_Type
tasks:
  - id: mysql_tabkyc_type_search
    fn: com.gs.datastore
    args:
      datasource: mysql
      data: <% inputs.body %>
      config:
        method: tabKYC_Type.findMany
